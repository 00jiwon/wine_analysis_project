name: test1026
channels:
  - defaults
dependencies:
  - python=3.10
  - numpy
  - ipykernel
  - conda
  - pandas
prefix: /opt/anaconda3/envs/test1026

# 터미널 실행: conda env export --from-history > environment.yml